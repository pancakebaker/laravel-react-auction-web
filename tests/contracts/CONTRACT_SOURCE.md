# Canonical contract snapshot

The OpenAPI document in this directory is a pinned, test-only snapshot from
`pancakebaker/dbap-integration-contracts` at commit
`6c89fb85d96110b881ff789470e3d7acfd5331e1`.

It verifies the Laravel BFF's Bidding endpoint expectations without generating
clients or changing the runtime HTTP boundary.
